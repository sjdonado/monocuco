[project]
name = "monocuco-cli"
version = "0.1.0"
description = "Utility scripts for Monocuco dataset management"
readme = "README.md"
requires-python = ">=3.9,<3.13"
dependencies = [
    "click>=8,<9",
    "pyarrow>=16,<19",
    "pyarrow-stubs>=17.19",
]

[tool.uv]
package = false
